"""RA shared core: synthetic data generation, forecasting, and the decision
engine. This package has no dependency on FastAPI or React — it is imported
as-is by both the backend API (backend/app) and the notebook demo
(notebook/RA_notebook_demo.ipynb) so the two presentation layers can never
drift apart on numbers or logic.
"""
